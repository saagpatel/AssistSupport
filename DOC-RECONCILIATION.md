# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats
the code as read-only ground truth and edits only documentation so it reflects the repo's actual
current state. It changed no source code, config, tests, or lockfiles, and ran no builds or
tests. Its purpose is to leave an auditable record of every comparison made and every edit
applied so that a reviewer scanning the branch can verify the work without re-reading every file.

---

## Per-Claim Findings

### 1. What It Is

**Status:** consistent

**Evidence:** `README.md` intro paragraph, `AGENTS.md` Portfolio Context section, and `src-tauri/tauri.conf.json:2` (`"productName": "AssistSupport"`) all agree: a local-first macOS desktop app combining a Tauri 2 shell with an ML search pipeline to draft grounded IT support answers from an operator-owned knowledge base. `src-tauri/src/main.rs` and `src-tauri/src/lib.rs` confirm the Tauri entry-point and command-layer structure. The one-line descriptor in the README intro is accurate. No changes made.

### 2. Current State

**Status:** consistent

**Evidence:** `package.json`, `src-tauri/Cargo.toml`, and `src-tauri/tauri.conf.json` all show version `1.3.0`. `CHANGELOG.md:3` records `[1.3.0] (2026-05-31)` as the latest release. `git log --oneline -20` shows recent dependency-lock and CI activity consistent with active maintenance. `docs/status/current-health.md` last-audited date is April 21, 2026 — this is a human-authored date and is unverifiable against the current moment, so it is left unchanged.

The `docs/plans/current-remediation-plan.md` and `docs/plans/product-improvements-roadmap.md` both describe ongoing active work; the plans are forward-looking (unverifiable) but do not make claims that contradict the shipped code structure.

No changes made.

### 3. Stack

**Status:** drifted (two items fixed)

**Evidence and changes:**

**a) Version badge**

- `README.md:1` badge read `version-1.2.0`; `package.json:3`, `src-tauri/Cargo.toml:3`, and `src-tauri/tauri.conf.json:4` all state `"version": "1.3.0"`. `CHANGELOG.md:3` confirms `1.3.0` released 2026-05-31.
- **Fixed:** `README.md:1` — `version-1.2.0` → `version-1.3.0`

**b) LLM inference technology**

- `README.md:123` (stack table) and `AGENTS.md:80` (stack table) both stated `Local via Ollama (optional)`. A code search across `src-tauri/src/**` and `src/**` found zero occurrences of the string "ollama". `src-tauri/Cargo.toml:46` lists `llama-cpp-2 = "0.1"` as a direct dependency. `src-tauri/src/llm.rs:1–16` imports `llama_cpp_2::*` and documents "Embedded llama.cpp inference with Metal GPU acceleration". `src-tauri/src/commands/model_runtime.rs` and `src-tauri/src/commands/model_commands.rs` confirm the llama.cpp engine is the only LLM runtime path.
- **Fixed:** `README.md` stack table — `Local via Ollama (optional)` → `Local via llama.cpp (optional)`
- **Fixed:** `AGENTS.md` stack table — `Local via Ollama (optional)` → `Local via llama.cpp (optional)`

All other stack entries (Tauri 2 + Rust, React + TypeScript + Vite, TF-IDF/Logistic Regression/ms-marco-MiniLM-L-6-v2, SQLite encrypted via SQLCipher, IBM Plex Sans + JetBrains Mono) are confirmed consistent with `src-tauri/Cargo.toml`, `package.json` dependencies, `search-api/reranker.py:4`, and `search-api/intent_detection.py:3–4`.

### 4. How to Run

**Status:** consistent

**Evidence:** All commands documented in `README.md` Quick Start and Core Commands sections exist in `package.json` `scripts` block:

- `pnpm install` — standard Node.js install, confirmed by `package.json:1`
- `pnpm dev` — defined at `package.json:8` (`"dev": "vite"`)
- `pnpm tauri build` — defined at `package.json:11` (`"tauri": "tauri"`)
- `pnpm health:repo` — defined at `package.json:75`
- `pnpm health:release` — defined at `package.json:76` (`"health:release": "node scripts/ci/run-release-health.mjs"`)
- All `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:ci`, `pnpm ui:gate:regression`, etc. are defined.

`search-api/README.md` runtime setup instructions reference Python, pip, and the Flask sidecar — consistent with `search-api/search_api.py` and `search-api/wsgi.py` being present.

No changes made.

### 5. Known Risks

**Status:** consistent

**Evidence:** `README.md` states "Core SQLite data and token material are encrypted at rest via SQLCipher (AES-256); optional vector-search embeddings stay local but are not currently encrypted at rest when vector search is enabled." `src-tauri/Cargo.toml:33` (`bundled-sqlcipher`) and `src-tauri/src/llm.rs` confirm the SQLCipher path. `docs/SECURITY.md:19` repeats the vector-plaintext caveat verbatim. The risk statements are accurate. No changes made.

### 6. Next Move

**Status:** unverifiable

**Evidence:** The roadmap items in `docs/plans/product-improvements-roadmap.md` and the remediation findings in `docs/plans/current-remediation-plan.md` are forward-looking. Whether specific phases are complete or stubs is determinable only at runtime or by running tests, which this pass does not do. Left unchanged per the conservative rule.

---

## Contradictions for Manual Review

None. All drifted items were in editable files (`README.md`, `AGENTS.md`) and have been corrected above.

---

## Footer

- **Run date/time:** 2026-06-02 19:38:53 PDT
- **Branch:** docs/truth-up-2026-06-02
- **HEAD sha reconciled against:** f6b3ed3412f71ddb202a2cfda8d5bfedea2ac9de
