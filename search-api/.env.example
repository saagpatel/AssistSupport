# Search API environment configuration
# Copy this file to .env.production for production deployments.

# Runtime mode
ENVIRONMENT=development
# If 3000 is occupied locally, choose a free port (for example 3388).
ASSISTSUPPORT_API_PORT=3000

# Authentication
# In production, set a strong random value and never commit it.
ASSISTSUPPORT_API_KEY=dev-key-change-in-production

# Rate limit storage backend
# development: memory://
# production example: redis://127.0.0.1:6379/0
ASSISTSUPPORT_RATE_LIMIT_STORAGE_URI=memory://

# PostgreSQL
ASSISTSUPPORT_DB_HOST=localhost
ASSISTSUPPORT_DB_PORT=5432
ASSISTSUPPORT_DB_USER=assistsupport_dev
ASSISTSUPPORT_DB_PASSWORD=
ASSISTSUPPORT_DB_NAME=assistsupport_dev
